{
  "name": "@cancri/rules-tests",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Firestore + RTDB security-rules tests on the emulator — proves per-user isolation (ADR-0004).",
  "scripts": {
    "test": "vitest run"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^5.0.1",
    "firebase": "^12.15.0",
    "vitest": "^4.1.9"
  }
}
