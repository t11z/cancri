# Hoisted node_modules: avoids symlink friction when the repo lives on a
# Windows drive mounted into WSL/Docker (drvfs). Safe default for this monorepo.
node-linker=hoisted
auto-install-peers=true
strict-peer-dependencies=false
